<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Assignable roles
    |--------------------------------------------------------------------------
    |
    | Roles that can be assigned via the admin UI. The legacy "user" role is
    | excluded — only admin and employee are supported.
    |
    */

    'assignable' => ['admin', 'employee'],

];
